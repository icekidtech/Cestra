; Allow native post-install build scripts for these packages (pnpm v11+)
enable-pre-post-scripts=true

; Do not re-run a dependency status check before `pnpm run <script>`.
; pnpm 11's check otherwise fails the build over the harmless
; "Ignored build scripts" notice for @nestjs/core and bcrypt.
verify-deps-before-run=false
